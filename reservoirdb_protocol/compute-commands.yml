- name: CreateComputeCluster
  type:
    fields:
      name: ComputeClusterRef
    required:
    - name
  implements:
  - Command

- name: DeleteComputeCluster
  type:
    fields:
      name: ComputeClusterRef
    required:
    - name
  implements:
  - Command
