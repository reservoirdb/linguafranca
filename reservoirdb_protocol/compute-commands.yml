- name: CreateComputeCluster
  type:
    fields:
      name: ComputeClusterRef
    required:
    - name
  implements:
  - Command

- name: DeleteComputeCluster
  type:
    fields:
      name: ComputeClusterRef
    required:
    - name
  implements:
  - Command

- name: ActivateComputeCluster
  type:
    fields:
      name: ComputeClusterRef
    required:
    - name
  implements:
  - Command

- name: DeactivateComputeCluster
  type:
    fields:
      name: ComputeClusterRef
    required:
    - name
  implements:
  - Command
