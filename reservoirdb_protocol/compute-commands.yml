- name: CreateComputeCluster
  type:
    fields:
      name: ComputeClusterRef
  implements:
  - Command

- name: DeleteComputeCluster
  type:
    fields:
      name: ComputeClusterRef
  implements:
  - Command
