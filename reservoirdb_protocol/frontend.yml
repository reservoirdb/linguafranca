- name: UIState
  type:
    fields:
      tables: map<string, Table>
      schemas: map<SchemaRef, Schema>
      users: map<UserRef, User>
      roles: map<RoleRef, Role>
  implements:
  - TxnResult

- name: UIGetState
  type:
    fields: {}
  implements:
  - Command
