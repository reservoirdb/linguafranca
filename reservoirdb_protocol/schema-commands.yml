- name: CreateSchema
  type:
    fields:
      name: SchemaRef
    required:
    - name
  implements:
  - Command

- name: DeleteSchema
  type:
    fields:
      name: SchemaRef
    required:
    - name
  implements:
  - Command
